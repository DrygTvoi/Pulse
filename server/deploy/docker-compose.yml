version: "3.8"

services:
  pulse:
    build: .
    container_name: pulse-server
    restart: unless-stopped
    ports:
      - "443:443"            # WebSocket relay + decoy HTTPS
      - "80:80"              # ACME HTTP-01 challenge (tls_mode="auto")
      - "3478:3478/udp"      # TURN (UDP)
      - "3478:3478/tcp"      # TURN (TCP)
    volumes:
      - pulse-data:/data
      - ./config.toml:/etc/pulse/config.toml:ro
    command: ["serve", "--config", "/etc/pulse/config.toml", "--data-dir", "/data"]
    environment:
      - PULSE_LOG_LEVEL=info
    deploy:
      resources:
        limits:
          memory: 1G

volumes:
  pulse-data:
